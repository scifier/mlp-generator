.navbar.navbar-default.navbar-inverse.navbar-fixed-top
  .container
    .navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/')
        i.fa.fa-code
        | Dr. Calvin
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li(class=(title === 'Home') ? 'active' : undefined)
          a(href='/') Home
        //- li(class=(title === 'New network') ? 'active' : undefined)
        //-   a(href='/network-form') New network
        li(class=(title === 'Networks') ? 'active' : undefined)
          a(href='/networks') Networks
        li.dropdown(class=(title === 'New network') ? 'active' : undefined)
          a.dropdown-toggle(href='#', data-toggle='dropdown')
            span New network 
            i.caret
          ul.dropdown-menu
            li
              a(href='/network-form')
                i.fa.fa-fw.fa-eye
                | &nbsp; Visual mode
            li.divider
            li
              a(href='/expert-network-form')
                i.fa.fa-fw.fa-graduation-cap
                | &nbsp; Expert mode


